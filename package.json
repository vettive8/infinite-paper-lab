{
  "name": "infinite-paper",
  "version": "11.0.0",
  "private": true,
  "type": "module",
  "description": "Infinite Paper — file-backed markdown note canvas",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/"
  },
  "engines": {
    "node": ">=18"
  }
}
