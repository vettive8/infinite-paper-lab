# Version 2

This is the first experiment after `v1`.

Changes from `v1`:

- Notes no longer wrap automatically.
- A line only breaks when Enter is pressed.
- Browser storage is separate from `v1`, so both versions can be tested independently.
